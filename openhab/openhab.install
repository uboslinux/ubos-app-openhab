_user="openhab"

post_install() {
    fixPermissions
}

post_upgrade() {
    fixPermissions
}

fixPermissions() {
	chown -R ${_user}:${_user} /etc/openhab
	chown -R ${_user}:${_user} /ubos/share/openhab
	chown -R ${_user}:${_user} /ubos/lib/openhab
	chown -R ${_user}:${_user} /var/log/openhab
}
